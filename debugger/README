To generate the Makefile for the debugger object that will be linked in, run the generate.sh script. This script takes two arguments: the path to the compiler in the patched toolchain and the name for the debugger object.

Example: ./generate.sh ~/diablo/toolchain-gcc-4.8.1-arm/bin/arm-diablo-linux-gnueabi-cc debugger.o
